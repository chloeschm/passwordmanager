# Simple Password Manager :)
ONLY for fun, personal use. Not for real passwords that you need to keep safe!

## AES Encryption
Uses Python library PyCryptoDome as the encryption method

## Hash Verification
Uses SHA256 Hash Function to verify the user at login. The password entered at point of login is compared to the stored master password, and access is granted only if the hashes match.
